{% extends "::base.html.twig" %}

{% block title %}Wybierz Podrząd:{% endblock %}

{% block body %}
    {% set i = 0 %}
    <table id="dinoSel">
        {% for suborder in suborders %}

            {% if i == 0 or i == 3%}
                <tr>
            {% endif %}
            <td>
                <a href="{{ path('selectSuborder', {'id': suborder.id, 'selection': selection}) }}"><img src='{{ asset( suborder.getWebPath ) }}' width="100" height="80"></a><br>
                {{suborder.name}}
            </td>
            {% if i  == 2 or i == 5%}
                </tr>
            {% endif %}
            {% set i = i + 1 %}
        {% endfor %}
    </table>
{% endblock %}